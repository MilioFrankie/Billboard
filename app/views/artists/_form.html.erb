<h1><%= @artist.id ? "Edit Artist" : "New Artist" %></h1>
<hr />
<%= form_for [@billboard, @artist] do |f| %> <%= f.label :name %> <%=
f.text_field :name, required: true, autofocus: true, placeholder: "Name" %>
<br />
<%= f.submit %> <% end %>
